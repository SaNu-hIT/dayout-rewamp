/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    // Allow local images from public folder (default)
    // Add any external domains here if needed
    formats: ['image/webp'],
  },
};

export default nextConfig;
